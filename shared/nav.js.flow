// @flow
import {Component} from 'react'

import type {VisibleTab, Tabs} from './constants/tabs'

export type Props = {
  menuBadge: boolean,
  provisioned: boolean,
  username: string,
  switchTab: (tab: Tabs) => void,
  folderBadge: number,
  children: React$Element<*>,
  routeSelected: VisibleTab,
}

export default class Nav extends Component<void, Props, void> {}
